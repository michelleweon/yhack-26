# Raw Assets

This folder holds source and reference media that should not be imported directly by the main app.

- `images/backgrounds/`: background art source files and variants
- `images/mockups/`: mockups and visual references
- `videos/`: reference clips and animation source files

Runtime assets used by the Vite app belong in `client/src/assets/`.
